pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.{ts,tsx,js,jsx,css,json}"
      run: pnpm oxfmt --check {staged_files}
    - name: lint
      glob: "*.{ts,tsx,js,jsx}"
      run: pnpm oxlint {staged_files}
